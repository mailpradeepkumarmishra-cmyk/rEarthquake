% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_timeline_label.R
\name{geom_timeline_label}
\alias{geom_timeline_label}
\title{Add labels to an earthquake timeline}
\usage{
geom_timeline_label(
  mapping = NULL,
  data = NULL,
  stat = StatTimelineLabel,
  position = "identity",
  n_max = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{ggplot2::aes()}.}

\item{data}{A data frame containing earthquake data.}

\item{stat}{Statistical transformation to use.}

\item{position}{Position adjustment. Defaults to \code{"identity"}.}

\item{n_max}{Maximum number of earthquakes to label. The earthquakes
with the largest \code{EQ_PRIMARY} values are selected.}

\item{na.rm}{Logical indicating whether missing values should be
silently removed.}

\item{show.legend}{Logical indicating whether the layer should
appear in the legend.}

\item{inherit.aes}{Logical indicating whether to inherit aesthetics
from the plot.}

\item{...}{Additional arguments passed to the layer.}
}
\value{
A ggplot2 layer containing earthquake labels.
}
\description{
Adds labels to earthquake points on a timeline. A vertical line
connects each earthquake to its label.
}
\details{
When \code{n_max} is supplied, only the \code{n_max} earthquakes with the
largest magnitudes are labelled.

The x aesthetic should contain earthquake dates and the label
aesthetic should contain the variable used for annotation,
such as \code{LOCATION_NAME}.
}
\examples{
\dontrun{
ggplot2::ggplot(
  earthquake_data,
  ggplot2::aes(
    x = DATE,
    y = COUNTRY,
    label = LOCATION_NAME
  )
) +
  geom_timeline_label(n_max = 5)
}

}
