% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_timeline.R
\name{geom_timeline}
\alias{geom_timeline}
\title{Draw a timeline of earthquakes}
\usage{
geom_timeline(
  mapping = NULL,
  data = NULL,
  stat = "timeline",
  position = "identity",
  xmin = NULL,
  xmax = NULL,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{ggplot2::aes()}.}

\item{data}{A data frame containing earthquake data.}

\item{stat}{Statistical transformation to use. Defaults to
\code{StatTimeline}.}

\item{position}{Position adjustment. Defaults to \code{"identity"}.}

\item{xmin}{Minimum date to display.}

\item{xmax}{Maximum date to display.}

\item{na.rm}{Logical indicating whether missing values should be
silently removed.}

\item{show.legend}{Logical indicating whether the layer should
appear in the legend.}

\item{inherit.aes}{Logical indicating whether to inherit aesthetics
from the plot.}

\item{...}{Additional arguments passed to the layer.}
}
\value{
A ggplot2 layer representing an earthquake timeline.
}
\description{
Creates a custom ggplot2 geom for displaying earthquakes on a
horizontal timeline. Each earthquake is represented by a point.
Multiple timelines can be created by mapping a variable to \code{y},
such as country.
}
\details{
The x aesthetic should contain earthquake dates. The optional
\code{xmin} and \code{xmax} arguments restrict the earthquakes displayed
to a specified date range.
}
\examples{
\dontrun{
ggplot2::ggplot(
  earthquake_data,
  ggplot2::aes(
    x = DATE,
    y = COUNTRY
  )
) +
  geom_timeline()
}

}
